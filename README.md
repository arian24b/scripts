# scripts

Server bootstrap utility: `server-setup.sh`

Usage (short):
```
sudo ./server-setup.sh server --wizard
sudo ./server-setup.sh server --preset hardened -L auto -i uv,docker -U
```

Help:
```
./server-setup.sh --help
```
